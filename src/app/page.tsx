import Link from "next/link";
import {ArrowRight} from "lucide-react";
export default function Home(){return <main className="landing"><header className="topbar"><span className="brand">BorderBooks</span><Link className="button small" href="/app">Open workspace <ArrowRight size={14}/></Link></header><section className="landing-main"><p className="eyrow eyebrow">Invoice reconciliation, without the black box</p><h1>Match invoices to money movements with an audit trail.</h1><p className="landing-copy">BorderBooks deterministically matches uploaded invoice and bank or payout files—then shows exactly why every pair was proposed.</p><Link className="button" href="/app/new">Start a match run <ArrowRight size={16}/></Link><div className="trustline"><span>Never moves money</span><span>Not accounting advice</span><span>No bank scraping</span><span>Uploads are not used for model training</span></div></section></main>}
